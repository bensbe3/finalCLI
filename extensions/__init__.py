"""
extensions/ — clean seams for future work. Nothing here is implemented.

Each module in this folder holds ONE no-op function with the signature it
will eventually need, so the feature can be added later without moving or
restructuring anything. Their intended call sites are written out (as
comments) near the top of main.py.

  ai_advisor.py   - AI recommendation engine
  history.py      - scan history / delta tracking between runs
  correlation.py  - cross-scanner risk correlation
"""
